<!DOCTYPE html>
<html>
<head>
  <title>First_app</title>
  <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
  <!--[if lt IE 9]>
    <script src="../assets/js/html5shiv.js"></script>
  <![endif]-->
  <%= stylesheet_link_tag "application", :media => "all" %>
</head>
<body>
  <div class="container"> 
    <div class="masthead">
      <h3 class="muted">$n!gDh@ R@Y</h3>
      <div class="navbar">
        <div class="navbar-inner">
          <div class="container">
            <ul class="nav"> 
              <li class="<%= request.original_url == root_url ? 'active' : '' %>"><a href="<%= root_path %>">Home</a></li> 
              <li class="<%= request.original_url == projects_url ? 'active' : '' %>"><a href="<%= projects_path %>">Projects</a></li>
              <li class="<%= request.original_url == about_url ? 'active' : '' %>"><a href="<%= about_path %>">About</a></li>
              <li class="<%= request.original_url == contact_url ? 'active' : '' %>"><%= link_to "Contact", contact_path %></li>
            </ul>
          </div>
        </div>
      </div>
    </div>
    <div class="container">
      <% flash.each do |key, value| %>
        <div class="<%= flash_class(key) %>">
              <%= value %>
        </div>
      <% end %>
    </div>
    <%= yield %>
  </div>
  <%= javascript_include_tag "application" %>
</body>
</html>
